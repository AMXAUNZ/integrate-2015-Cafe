./src/mod/rms-sdk

This directory is for AMX module files and AMX driver files belonging to the RMS SDK.
E.g., *.axs, *.tko, *.jar, .xdd
Files should be stored within sub-directories.

NOTE: Due to licensing restrictions the RMS SDK cannot be uploaded to GitHub. It will need to be downloaded from the AMX website (www.amx.com)